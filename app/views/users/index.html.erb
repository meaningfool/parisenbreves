<h1>Users</h1>

<ul>
	<% for @user in @users %>
		<li>
			<%= link_to(@user, :id => "show_user_#{@user.id}") do %> 
				<h2 class="user_name"><%= @user.name%></h2>
			<% end %>
		</li>	
	<% end %>
</ul>