<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "Paris en brèves", root_path, id: "logo" %>
      <nav>
        <ul class="nav pull-left">
          <li><%= link_to "Accueil", root_path, :id => "home" %></li>
          <li><%= link_to "Brèves", content_path, :id => "content" %></li>
          <li><%= link_to "Contribuer", contribute_path, :id => "contribute" %></li>
        </ul>
        <ul class="nav pull-right">
          <% if signed_in? %>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%= current_user.name %><b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profil", current_user %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Déconnexion", signout_path, method: "delete", :id => "sign_out" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "S'inscrire", signin_path %></li>
            <li><%= link_to "Connexion", signin_path, :id => "log_in" %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>