<% content_for(:head) do %>
    <%= javascript_include_tag 'home' %>
<% end %>

        <div id="this-carousel-id" class="carousel slide" data-interval="8000"><!-- class of slide for animation -->
            <div class="carousel-inner">
                    <% @carousel_features.each_with_index do |breve, index| %>
                        <div class="item <%= index == 0 ? 'active' : '' %>"><!-- class of active since it's the first item -->
                            <%= peb_image_tag breve, :large  %>
                            <div class="carousel-breve">
                                <div class="breve-item">
                                <h2><%= breve.title %></h2>
                                <span><%= breve.location %></span>
                                <p><%= breve.description %></p>
                                </div>
                                
                            </div>

                        </div>
                    <% end %>  
                                   
            </div><!-- /.carousel-inner -->
            <%= link_to "Voir l'ensemble des brèves", published_path, :class => "content_link" %>

            <div class="carousel-principes">
                <%= link_to contribute_path do %>
                    <div>
                        <div class="principes-icons"><i class='icon-quote-left'></i><span> ... </span><i class='icon-quote-right'></i></div>
                        <span class="principes-text">Les imaginaires des lieux de Paris racontés... en brèves</span>
                    </div>
                <% end %>
                <%= link_to contribute_path do %>
                    <div>
                        <div class="principes-icons"><i class='icon-group'></i></div>
                        <span class="principes-text">Un projet associatif et contributif.</br>Rejoignez-nous !</span>
                    </div>
                <% end %>
                <%= link_to contribute_path do %>    
                    <div>
                        <span class="creative-commons">CC</span>
                        <span class="principes-text">Des contenus sous licence</br>Creative Commons</span>
                    </div>
                <% end %>   
            </div> 
               
          <!--  Next and Previous controls below
                href values must reference the id for this carousel -->
            <a class="carousel-control left" href="#this-carousel-id" data-slide="prev">&lsaquo;</a>
            <a class="carousel-control right" href="#this-carousel-id" data-slide="next">&rsaquo;</a>
        </div><!-- /.carousel -->

        